# 交接提示词（2026-09-13 → 新会话续开发）

> 用法：新电脑/新会话 clone 或进入本仓库后，把下面整段作为首个提示词发给 AI 助手
> （或直接说「读 docs/交接提示词-2026-09-13.md 并按其执行」）。

---

我在开发一款 PDF 双语对照阅读器（Tauri 2 + React/TS 前端 + Python FastAPI sidecar 后端，
学术工具，开源复现，分支 `phase-1`）。请先读 `docs/README.md`（文档唯一入口）→
`docs/重构路线图.md`（状态跟踪表）→ 再按需读各阶段文档，然后等我指派任务。

## 项目当前状态（截至 2026-09-13 下午）

- **2026-09-11 里程碑不变**：阶段 1/2/3/5/6/8/10 人工验收通过；tag `v0.12.0` 已发布。
- **2026-09-12/13 已完成并推送**：
  - 阶段11 全部任务（T0–T6）：T5 翻译任务自动重接管（F5 恢复/上传即入库/
    BabelDOC 静默重接管+视图洞修复）、T6 进入时机 31% 门禁（进度卡/标题栏
    tab/继续翻译弹窗三入口统一）；
  - 用户反馈闭环：①弹窗蒙版 portal 修复 ②启动 splash（visible:false+静态
    品牌页+挂载后 show）③标题识别两级修复（文本规则收窄+几何检测架构
    `ocr/title_detect.py`：页0最大字号+最靠上行+metadata互证，用户论文
    8 篇 8/8）④进入时机；⑤F5 对照视图洞 ⑥进度双源标名+冻结 tab ⑦对照
    视图跨文档污染（babeldocStore 按路径归属采信）；
  - 阶段9-T6 **随包捆绑**定稿（两次迭代后用户拍板：目标用户非技术向、
    体积不敏感）：`build-babeldoc-runtime.ps1` 暂存（embeddable python+
    venv site-packages）→ `build-exe.ps1` 经 tauri `-c` 配置注入 resources →
    落位后端 exe 同级；检测链 venv→exe 同级→数据目录；下载链路已整体移除。
- **2026-09-13 打包白屏事故链（已根治，复盘见路线图 §4.1 末）**：
  - 根因 A：环境重建时 pymupdf 栈漂移到 1.28.2，Layout 引擎硬依赖
    onnxruntime，onefile 冻结环境导入即原生段错误 → **钉版本
    pymupdf==1.26.4 / pymupdf4llm==0.0.27 进 pyproject**（0.0.27 天然
    classic = textlayer 全部规则的调优基线），TEXT_LAYER_MODEL v18 熔断；
  - 根因 B：App.tsx 就绪门早退分支写在后续 hook 之前（hook 数量跨渲染
    不一致，就绪翻转时 React 崩树）→ hooks 全部前移；
  - 修复配套：冻结态 stderr 落盘 `logs/backend-sidecar.log`（TextIOWrapper
    + faulthandler，放 main.py 模块顶部）、`/api/log` 补 await（此前 500
    吞掉全部前端错误）、就绪探测改 Rust `backend_health` 命令、启动窗口
    尺寸钳制工作区 90%+居中。
- **当前最新安装包**（13:16，含上述全部）：`src-tauri/target/release/bundle/
  nsis/PDF双语阅读器_1.0.0_x64-setup.exe`（216MB）。**用户尚未完成验收**。
- **验证基线**：pytest 153/6、tsc 0 错误、vite build、cargo check 全绿。
- 旧进程残留问题：退出应用偶尔残留 pdf-backend.exe（RunEvent::Exit 某些
  路径不触发），已登记待办（见路线图）。

## 剩余计划（全部）

1. **用户安装验收**（13:16 包）：首启等待→进文献库；窗口自适应；左右对照
   开箱即用；旧文档缓存失效确认→重提取后标题正常。**若异常：读
   `%APPDATA%/pdf-reader/logs/frontend.log` 与 backend-sidecar.log（已落盘）**。
2. **登记待办**：退出进程残留清理（启动前 kill 残留 sidecar 或退出钩子加固）。
3. **阶段11 人工跨模式复验** → 通过后打 **v0.13.0** 收官阶段9/11。
4. **候选改进两项（等拍板）**：① /reader 无会话路由守卫；② 打包版拦截
   F5/Ctrl+R。
5. **可选项（无排期）**：阶段2-T5 SSE 流式推送。
6. **体检语料注意**：`tools/extraction_health.py` 的 `test_ocr/` 语料目录
   本机缺失（旧机产物未同步）——跑体检前需补齐 7 篇 PDF。

## 环境要点（本机环境已建好，无需重建）

- 后端：`.venv\Scripts\python.exe backend/main.py`（127.0.0.1:8000）；
  前端 `npm run dev`（5173）或 `npm run tauri dev`；一键 `scripts\dev-start.ps1`
- **依赖用 uv 管理、版本已钉 pyproject**（pymupdf==1.26.4 / pymupdf4llm==
  0.0.27，勿升级——升级前必过 tests + extraction_health 全量体检）；
  `.venv-babeldoc`（BabelDOC==0.6.4）
- WiX 缓存 `%LOCALAPPDATA%\tauri\WixTools314`；`build/babeldoc-runtime/`
  为运行时暂存（800MB，tauri resources 捆绑源，删了可用脚本重建）；
  `dist/` 是 vite 产物（每次构建重生成，勿放任何要保留的东西——emptyOutDir）
- 用户数据 `%APPDATA%/pdf-reader/`（config.json 带 key；cache/ 含翻译缓存
  与 babeldoc 产物；logs/ 前端+sidecar 日志）；测试论文在
  `E:\ZiLiao\论文阅读\GraphRAG&KGQA\`（8 篇）
- 模型：Qwen/Qwen3-8B 翻译 + PaddleOCR-VL-1.5（OCR max_tokens ≤8192）

## 工作约定（强约束）

- **有一定修改就及时提交 git**；**运行中的服务不可擅动**（重启后端/查杀
  端口/压测前必须确认用户没在用，README §4.7）
- UI 改动先出 mockup 确认；蓝色仅交互/翻译语义；原生对话框一律自绘
- 阶段完成回填阶段文档 + 路线图状态表；接口改动同步总纲 §4.4
- 用户截图反馈问题，先根因分析再修复；人工测试优先，不做自动部署
- 模型配置禁止硬编码；OCR max_tokens ≤8192；API key 不进日志/记忆/对话
- **打包相关三坑（2026-09-13 实测）**：tauri.conf.json 的 resources 恒为
  `[]`（静态声明目录缺失即炸 build script），正式包由 build-exe.ps1 经
  `-c` 注入；运行时暂存放 `build/` 不放 `dist/`（vite emptyOutDir 会清空）；
  打包前确认旧版应用与 pdf-backend.exe 进程已退出（否则安装器写文件失败）

## 建议执行顺序

用户安装验收（13:16 包）→ 异常则读 logs 定位 → 通过后阶段11 人工复验 →
v0.13.0 收官（release 检查清单见版本规划.md §5）→ 候选改进拍板。
