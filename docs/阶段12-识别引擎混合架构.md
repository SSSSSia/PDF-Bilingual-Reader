# 阶段 12：识别引擎混合架构——VLM 结构化解析（v0.14.0）

> **状态**：🔶 T1~T8 代码完成门禁全过；人工验收中（FG-RAG 重翻反馈三根因 → 立项 T9 版面模型信号源，决策证据已采，见 §3.1 与 `docs/版面检测信号源对比.md`）｜ **前置**：无硬依赖（阶段 11 可并行；本阶段不碰其改动面）｜ **依据**：`docs/VLM结构化解析对比.md`（2026-09-14 A/B 实测，决策门已过）+ 阶段4 评估预注册的「~80 分天花板」+ `提取质量体检.md` 标题根治二期的补丁跑步机复盘
> **目标一句话**：数字页的文本与公式提取主路径换成「区域快照+遮罩 → PaddleOCR-VL 整页结构化解析 → 交叉校验降级」混合架构，公式以 LaTeX 呈现、复杂版式页内容完整度提升——重排版左右对照/紧跟/原版对照三模式共用管线，一次受益。

---

## 1. 背景与证据

- **A/B 实测（4 篇真实论文 × 12 页，2026-09-14）**：PaddleOCR-VL-1.5 整页结构化解析 vs 现行 textlayer（pymupdf4llm classic + 启发式，v18）——
  - 公式：**0 → 40 段定界 LaTeX**（`\(...\)`/`\[...\]`，与前端 `preprocessMath`→KaTeX 链路天然兼容）；Attention p4 招牌公式逐字符正确；
  - 复杂版式页内容完整度（bag F1）：DALK p1 0.976→0.996、FG-RAG p1 0.960→0.997、Survey p1 0.977→0.995——正是现行补丁最吃力的标题/作者区页型；
  - 双栏顺序：可测 5 页 B 1.0×4+0.79 vs A 1.0×3+0.667×2（反例 DALK p2 整栏交换，内容完整，需几何兜底）；
  - 表格：hosted 版整页提示下**全部丢弃**（元素级「Table Recognition:」实验也不可用）→ 维持 v5 用户决策（表格=快照图）+ 送 VLM 前打白遮罩；
  - 截断 0/12 页、平均 6.4s/页、两轮重跑数值一致（近似确定性）、免费档零成本。
- **现行架构天花板实证**：TEXT_LAYER_MODEL v2→v18 十六个版本的补丁迭代；标题根治二期自认「每条规则都从个案倒推、只对见过的形态负责」；阶段4 评估预注册的 Markdown 重建路线 ~80 分天花板已被 v13→v18 的实战验证。
- **三模式共用管线**：`parallel`（bilingual/inline 两子模式）与 `original_click` 的 bbox 标注、译文全部消费同一份提取+翻译产物；`original_bilingual`（BabelDOC）已效果好且不动。
- **翻译侧独立证据**（WMT25 findings + BabelDOC/沉浸式翻译生产实现，2026-09 调研）：`<<<n>>>` 分隔符批量是已知反模式（弱模型上回声/段融合高发），生产级做法是 JSON 数组结构化 I/O + 单段回退 + temperature=0。

## 2. 范围

**做**：VLM 解析模块、图表区域遮罩与快照协同、文本层交叉校验降级、管线接入与缓存熔断、噪音过滤与顺序几何兜底、翻译协议升级（JSON 数组+单段回退）、翻译质量档位（可选配置，默认档保持免费）
**不做**：表格结构化转 markdown/HTML（维持快照，远期再评估）；本地布局模型 / MinerU 自建端点；BabelDOC 解析能力内嵌复用（AGPL+内部 API，维持子进程导出定位）；重排版前端渲染改造（KaTeX/markdown 管线已就绪）；原版对照模式 UI 改动

## 3. 任务分解

- [ ] **T1 VLM 解析模块（新建 `backend/ocr/vlm_parse.py`）**
  - 从 `tools/ab_textlayer_vs_vlm_parse.py` 的验证代码转正：`_vlm_call`（payload 与生产扫描页通道完全一致，保留 finish_reason）、截断按栏对半重发（栏检测复用几何判定，单栏页上下对半）；
  - 页级缓存沿用 `ocr_key(pdf_hash, page, model)`（伪模型名独立于 textlayer/视觉 OCR 缓存，含版本后缀）；
  - 单测：mock httpx 响应的截断重发、空输出、HTTP 错误路径。
- [ ] **T2 区域遮罩与快照协同（`backend/ocr/textlayer.py` 提取函数旁路新增）**
  - 快照照旧（`_snapshot_figures` 不动，v5 表格=快照决策维持）；新增「遮罩渲染」：用 `_figure_regions`（光栅+矢量簇+find_tables，含 `_MERGE_GAP` 合并与外扩/表头吸收后的最终区域）在页面副本上 `draw_rect` 白底，再渲染送 VLM——实验已证不能只用 `get_image_rects`（矢量图标签在光栅 bbox 外）；
  - sidecar JSON 与 caption 锚定插回逻辑保留（快照引用插入 VLM 输出的 markdown，锚定用 VLM 输出中的 caption 文本，`_find_para_pos` 复用）。
- [ ] **T3 交叉校验降级（新建 `backend/ocr/verify.py` 或并入 vlm_parse）**
  - 真值=该页文本层字符流**扣除快照区域文本**（复用 `_figure_inner_text_rects` 的区域文本提取）；bag F1 < 0.90 的页回退现行 textlayer 输出并日志留痕（最坏情况=现状，内容完整兜底）；
  - 阈值可配；降级计数进 job stats（前端进度页可见降级页数）。
- [ ] **T4 管线接入与缓存熔断（`backend/pipeline/processor.py`）**
  - `_load_or_run_ocr`：数字页优先 VLM 路线（遮罩→解析→校验→降级），扫描页维持既有视觉路线（同模型同提示，行为天然统一）；
  - `TEXT_LAYER_MODEL` v18→v19 熔断（翻译缓存按 text_hash 键控，未变化文本免重译，沿用 v17/v18 经验）；
  - 首页可见时延验收：页级流式下 ≤10s。
- [ ] **T5 顺序兜底与噪音过滤（`textlayer.py` / `processor.py` 小改）**
  - 高置信双栏页（几何列检测触发条件与 `_column_reading_order` 一致）对 VLM 输出段落做顺序校验/重排（DALK p2 整栏交换反例的兜底）；
  - 残留页码/孤立数字行由现有 `split_into_blocks` 的 `_NOISE_BLOCK` 处理，验证覆盖；
  - 保留：标题几何检测、页眉剔除、跨页续段合并、`_GENERIC_HEADINGS`；预期退役（按回归数据逐项确认后删）：`_demote_sentence_headings`/`_fix_italic_superscripts`/`_split_glued_columns`/`_column_reading_order` 主路径（降级为 T5 兜底）/前端 `mathFragment.ts` 碎片启发式。
- [ ] **T6 翻译协议升级（`backend/translate/providers/openai_compat.py`、`translate/sanitize.py`）**
  - `<<<n>>>` 批量改 JSON 数组结构化 I/O：输入 `[{"id":n,"text":...}]`，要求返回同长度数组（id 对齐），解析失败/缺段/段融合 → 仅出错段逐段重发（BabelDOC 同款回退）；
  - `temperature` 0.2→0；上下文注入改为「文档标题+当前小节标题+命中的术语条目」（替代全篇术语表注入）；`PROMPT_VERSION` pv5→pv6 熔断；
  - 单测：JSON 解析、缺段回退、段融合触发路径（替换现有 `parse_segments` 用例）。
- [ ] **T7 翻译质量档位（`backend/config.py` 默认值 + 前端 `ConfigPage.tsx`）**
  - 默认档保持免费 Qwen3-8B（免费卖点不破）；设置页增加「质量档位」说明与可填示例（DeepSeek-V4-Flash，¥1/M token ≈ 每篇论文约 ¥0.1），不改变用户自配任何 OpenAI 兼容端点的能力。
- [ ] **T8 回归与验收工具（`backend/tools/`）**
  - `ab_textlayer_vs_vlm_parse.py` 固化为回归工具（语料路径参数化）；`extraction_health.py` 语料补齐并加 VLM 路线列；补 `test_vlm_parse.py`/`test_openai_compat.py` 用例；pytest/tsc/compileall 门禁。

> 依赖：T1→T2→T4 主线串行；T3 与 T2 并行；T5 在 T4 后；T6/T7 与主线并行无依赖；T8 收口。

### 3.1 验收期任务（人工验收反馈立项，2026-09-14）

- [x] **T-a 字号几何证据（已提交 fb182b5，降级为 T9 兜底）**
  - `collect_font_evidence`/`apply_font_evidence`：粗体+字号判标题、编号深度定级、首页小字印刷块剔除；4 版式语料标定。
  - 验收期试修中发现规则数量随个案增长（协作协议第 9 条叫停）——保留为**版面模型不可用时的自动兜底**，不再新增规则。
- [x] **T9 版面模型为主结构信号（✅ 代码完成 2026-09-14，5 个提交；完成记录见 §7.2，T10 用户走查待做）**
  - 背景：FG-RAG 重翻验收三根因（无标题层级/无框表碎行/版权噪音）均为结构信号缺失；DocLayout-YOLO（随包 BabelDOC 运行时自带）在语料+未见论文上实测三根因全覆盖（title/abandon/table 全命中，泛化验证通过）。
  - T9.1 版面检测 worker：✅ `ocr/layout_worker.py`（跑随包运行时，NDJSON 逐页流式，扫描页跳推理，结构化错误上报）+ `ocr/layout_model.LayoutProvider`（缓存优先/伪模型名 `doclayout-v1`/静默超时与提前退出守卫）；**未进主后端 venv**（onnxruntime 隔离达成）。
  - T9.2 区域接入：✅ abandon 三用（遮罩+truth 扣除+输出剔除，textlayer 降级路径同样消费）+ title 提升与 **run-in 拆分**（T9.5 回归新增，见 §7.2）+ table/figure 并入 `_figure_regions` 候选与 tab_* 分类；`VLM_PARSE_MODEL v1→v2`、`TEXT_LAYER_MODEL v19→v20` 熔断。
  - T9.3 重复框去重：✅ `dedupe_regions` 纯几何（同 label IoU>0.6 保最高置信；**置信下限实测修正为 0.5**——任务草案 0.6 会把 FG-RAG p3 第三张无框表（0.56，与 0.38 复检框同 bbox）整体滤掉，违反验收标准 1；Algorithm 伪代码框实测 ≤0.42，0.5 落在实测空档 (0.42, 0.56]）。接线在 `regions()` 读取口：缓存保持模型原始产出，去重规则升级免失效缓存。
  - T9.4 兜底：✅ 版面信号缺席（`layout_ok=False`）自动落回 fb182b5 冻结版字号证据；**版面模型成功出区域的页即使无 title 也不用字号规则越权**（「本页无标题」是模型可信判定）；采集失败=无兜底不阻断。
  - T9.5 回归：✅ worker 真实链路冒烟 + DALK p9 + FG-RAG p1/p3 + 未见论文 TransE + A/B 工具 Route B 接 provider，量化结果见 §7.2；FG-RAG 删缓存全文重翻走查归 T10（用户）。
  - T9 验收标准（自动化部分判定，1-3 的用户走查归 T10）：
    1. FG-RAG 重翻：标题层级恢复（`#`/`##`/`###` ≥4 个且含文档标题与节标题）；版权/引用/venue 段落残留 0；p3 三张无框表以快照呈现。
    2. DALK p9 无框表以快照呈现（不再触发 bag 降级）。
    3. 泛化：未见论文（TransE 或用户任选）标题/噪音表现不劣于调优集。
    4. 性能：页 0 就绪 ≤10s（版面检测 ~3.7s + VLM ~5s 串行预算内）；缓存命中 <1s。
    5. 兜底：改名/移除 babeldoc 运行时后管线自动落回字号证据，任务不失败。
    6. 门禁：pytest / `npx tsc --noEmit` / `python -m compileall backend/ -q` 全过。
- [ ] **T10 人工验收收口（在 T9 后）**
  - 原 §4 标准 7 三模式走查 + T9 验收标准 1-3 逐项核对，用户确认后合并打 tag。
  - 走查时需拍板的已知边界：FG-RAG p1「ACM Reference Format」引用块仍保留（模型判 title 0.88 + plain text 0.97，冻结字号证据 smallprint 不触发——正文字号；剔除需文本规则，协议第 9 条禁止。候选后续信号源：PP-DocLayoutV3 换源评估，非本阶段）。

## 4. 验收标准（全部满足才算完成）

1. **公式**：4 篇样本论文（DALK/FG-RAG/Attention/Survey）中数学 span ≥5 的页，VLM 路线输出定界 LaTeX 段数 ≥1；抽 20 段人工目检正确率 ≥90%。
2. **内容完整度**：交叉校验后终态 bag F1 ≥0.95 的页占比 ≥95%（A/B 脚本口径，快照区域文本已从真值扣除）。
3. **阅读顺序**：几何列检测触发的双栏页，顺序得分 =1.0 占比 ≥90%（T5 兜底生效后）。
4. **降级路径**：mock VLM 空输出/低质输出（bag <0.90）时管线自动回退 textlayer，任务不失败且 stats 记录降级页数。
5. **性能**：数字 PDF 上传后首页可见 ≤10s；同文件重跑（缓存命中）<1s。
6. **翻译协议**：JSON 协议下全篇段融合触发 0 次；回声块数不高于 pv5 基线；`pytest` 全过、`npx tsc --noEmit` 通过、`python -m compileall backend/ -q` 通过。
7. **人工验收**：DALK 与 FG-RAG 删除缓存全文重翻，三模式走查（公式渲染/段落顺序/表格快照/标题/图表位置），用户确认通过。

## 5. 风险与缓解

| 风险 | 缓解 |
|------|------|
| hosted 模型升级导致输出格式漂移（`\(\)`→其他定界等） | 缓存键含模型名+版本后缀；交叉校验兜底；A/B 工具固化为回归 |
| 免费档限速（RPM/TPM）影响长文档 | 沿用并发上限+429 退避；页级缓存；长文档「纯文字页本地提取」按需触发列为后续优化位（不在本阶段） |
| 表格页交叉校验误降级（真值含表格文本而 VLM 有意丢弃） | T3 真值扣除快照区域文本后再比对 |
| VLM 顺序偶发整栏交换（DALK p2 实测） | T5 几何列序兜底（高置信双栏页强制/校验） |
| 首页时延从 ~1s 变 ~7s 的体验回退 | 页级流式保持渐进呈现；进度卡文案标注「结构化解析中」；缓存后秒开 |
| 首跑批翻译协议切换引入新失败形态 | T6 单测覆盖缺段/融合/非 JSON 输出路径；pv6 熔断可整体回退 pv5（配置项保留旧协议代码一个版本） |
| 打包回归 | 无新增依赖（httpx/pymupdf 均已在 sidecar）；build-exe 照常回归 |

## 6. 交付物

1. 代码改动（`backend/ocr/vlm_parse.py` 新建；`textlayer.py`/`processor.py`/`openai_compat.py`/`sanitize.py`/`config.py`/`ConfigPage.tsx` 修改；`backend/tools/` 与 `backend/tests/` 扩充）。
2. 本文档完成记录回填（含补丁退役清单的最终确认）。
3. 人工测试清单（`开发流程管理.md` §4.2 模板）填写结果。
4. `dev → master` 合并 + `v0.14.0` tag。

## 7. 完成记录（2026-09-14 开发完成，待人工验收）

- **完成日期**：2026-09-14（T1~T8 单日完成，分支 phase-12，9 个提交）
- **改动文件清单**：
  - 新建：`backend/ocr/vlm_parse.py`（VLM 客户端/截断对半重发/双栏几何/遮罩渲染/页级准备/交叉校验降级/快照插回+顺序兜底）、`backend/tests/test_vlm_parse.py`（28 例）、`backend/tests/test_processor_ocr.py`（5 例）
  - 修改：`backend/ocr/textlayer.py`（拆出 `extract_page_md` 供降级复用；`_strip_broken_links` 修 redaction 悬空链接崩溃）、`backend/pipeline/processor.py`（`_load_or_run_ocr` 换窗口式 VLM 主路线、v18→v19 熔断、vlm_pages/vlm_fallback 统计、小节滚动跟踪注入翻译）、`backend/translate/providers/openai_compat.py`（JSON 数组协议 pv6、temperature=0、术语命中过滤、缺段/融合仅出错段逐段重发）、`backend/config.py`（免费默认档决策注释）、`src/components/MainPage.tsx`（进度卡「结构化解析原文」）、`src/components/ConfigPage.tsx`（质量档位说明）、`backend/tools/ab_textlayer_vs_vlm_parse.py`（固化回归：Route B=生产链路+语料参数化）、`backend/tools/extraction_health.py`（VLM 主路线表+语料落到实测目录）、`backend/tests/test_providers.py`/`test_phase2.py`（协议用例替换）
- **验收结果逐条记录**（量化实测，2026-09-14 真实 API）：
  1. **公式 ✅**：数学 span≥5 的页全部产出定界 LaTeX（DALK p5=8 段、FG-RAG p3=5 段、Attention p2=7/p4=23 段；全集 DALK 53/Attention 57/FG-RAG 5 段，textlayer 路线恒 0）；20 段人工目检 ≥90% 待标准 7 用户走查确认（A/B 期 Attention 招牌公式已逐字符核对正确）
  2. **内容完整度 ⚠️ 部分**：12 页 A/B 样本终态 bag(raw 口径) ≥0.95 占比 10/12=83%——差在 FG-RAG p3（0.899）与 Attention p4（0.948）两个数学页，系 LaTeX 表示 vs 真值 Unicode 碎屑的**口径稀释而非内容丢失**（字母数字口径 0.90+/0.98）；DALK p9 无框表降级页终态=textlayer bag 0.992。若按字母数字口径（T8 校准，与校验阈值同基）12/12 ≥0.90
  3. **阅读顺序 ⚠️ 部分**：可测双栏页 order=1.0 占比 3/4=75%（差 FG-RAG p1=0.79：VLM 改写措辞致段落前缀匹配失败，`_column_reading_order` 保守跳过——宁可不重排也不搅乱的设计取舍）；DALK p2 整栏交换反例实测修复（raw=False→final=True）
  4. **降级路径 ✅**：mock 空输出/低质/API 失败/降级自身异常 4 条路径单测覆盖；stats.vlm_fallback 记录；低质原始解析不落缓存→下次运行自然重试（实测 hosted 对密集参考文献/表格页偶发只回页码 bag≈0.002，DALK p6/8/10/12 与 Attention p6 复现，重试后 Attention p6 自愈）
  5. **性能 ✅**：首页就绪实测 4.7s（Attention p4 真实调用）/0.7s（缓存命中）≤10s；缓存命中页毫秒级挂载；12 页并发 3 全量约 26s（页级流式渐进呈现）
  6. **翻译协议 ✅（代码级）**：Qwen3-8B 实测 5 段批次 JSON 5/5 id 对齐、0 段融合、`[[M0]]`/`\(\sqrt{d_k}\)`/术语（attention→注意力）全保留，6.6s/批；`pytest` 192 过 / `tsc --noEmit` 过 / `compileall` 过；全篇回声对比待标准 7 重翻实测
  7. **人工验收 ⬜**：DALK 与 FG-RAG 删缓存重翻三模式走查——待用户确认
- **性能对比数据（改动前后）**：提取单页 ~0.5s（本地 textlayer）→ ~5-7s（VLM 真实调用，免费档零成本）或 ~0.7s（缓存命中）；翻译批次协议 `<<<n>>>`→JSON 后好段不再随坏段重发，请求量下降；公式链路从「Unicode 碎屑+按需视觉识别」变为「定界 LaTeX 直接 KaTeX 渲染」
- **补丁退役最终确认**（T5，12 页语料回归证据）：`_demote_sentence_headings` 12/12 恒等、`_fix_italic_superscripts` 与 `_split_glued_columns` 对 VLM 输出为**负作用**（拍平正确上标¹²³/误拆标题块）——三者从主路径**架构性退役**（VLM 主路径不跑；保留于 textlayer 降级路径守 v18 底线）；`_column_reading_order` 主路径退役、降为 VLM 终态兜底（`_finalize_md` 内保守调用）；前端 `mathFragment.ts` 碎片启发式**暂留**（VLM 定界 LaTeX 与其正交，待标准 7 走查无回归后再议删除）
- **额外修复（顺手发现的真实 bug）**：apply_redactions 悬空链接项致 pymupdf4llm `get_links` 崩（Attention p10/Survey p5 整页提取 IndexError，v18 主路径同样中招会任务失败）——`_strip_broken_links` 修复；VLM 主路线天然绕开此类 pymupdf4llm 崩溃
- **遗留问题 / 跳过的任务及原因**：
  - 表格仍是快照（hosted 整页提示丢弃+元素级提示不可用，维持 v5 决策）；无框表 find_tables 检不出→遮罩/扣除缺失→该类页稳定降级 textlayer（DALK p6-9 表格群实测，终态=v18 水平：内容完整但表格文字不成表）——远期需版面模型级表格检测
  - 长文档全 VLM 化的免费档 RPM 压力：300 页文档提取约 10 分钟（并发 3）；「纯文字页本地提取按需触发」按风险表约定列为后续优化位
  - `original_click` 模式 bbox 标注依赖前缀匹配，VLM 改写措辞可能降低匹配率（不损失内容，仅高亮缺失）——标准 7 走查时专项观察

### 7.1 验收期反馈与 T9 立项（2026-09-14，新会话执行入口）

人工验收（FG-RAG 删缓存重翻）反馈「效果一般」，诊断三根因：①全篇零标题层级（VLM 平文本输出，管线无结构恢复机制）；②p3 无框表碎行混正文（find_tables 0 候选）；③版权/引用/venue 段落混入正文。先以字号规则试修（T-a，已提交 fb182b5），过程中规则随个案增长，用户按新立的协作协议第 9 条叫停（「技术栈不行就换技术栈、整体架构不行就换架构」），个案规则堆已全部撤销。随后按决策门流程采集证据：随包 BabelDOC 运行时自带的 DocLayout-YOLO 在 4 篇调优集 + 1 篇未见论文上实测三根因全覆盖（title/abandon/table 全命中、泛化通过、~1.2s/页），证据与方案对比固化为 `docs/版面检测信号源对比.md`，任务登记为 §3.1 T9（信号源更换，管线架构不变；字号证据降为兜底）。**新会话从 T9.1 开始执行**，前置确认：用户对 T9 决策拍板。

### 7.2 T9 完成记录（2026-09-14，代码完成待 T10 用户走查）

- **完成日期**：2026-09-14（同日 T9.1~T9.5，5 个提交：worker/接入层 → 区域接入 → 去重 → 兜底 → 回归修正）
- **改动文件清单**：
  - 新建：`backend/ocr/layout_worker.py`（随包运行时执行：OnnxModel 推理 → NDJSON 区域 JSON，2x 渲染坐标除回，扫描页 <120 字符跳推理）、`backend/ocr/layout_model.py`（LayoutProvider：缓存优先 + 子进程流式消费 + 静默超时/提前退出守卫 + `dedupe_regions`）、`backend/tests/test_layout_model.py`（13 例）、`backend/tests/test_layout_regions.py`（16 例）
  - 修改：`backend/ocr/textlayer.py`（`_figure_regions`/`_snapshot_figures` 增 `extra_regions`/`table_regions` 参数——版面 table/figure 区域进快照漏斗 + tab_* 分类）、`backend/ocr/vlm_parse.py`（区域分类/`_prepare` 三用采集/truth 扣除/遮罩扩展/`_drop_abandon_paragraphs`/`_promote_titles` 含 run-in 拆分/`_norm_split`/字号兜底接线；`VLM_PARSE_MODEL v1→v2`）、`backend/pipeline/processor.py`（`TEXT_LAYER_MODEL v19→v20`、provider 生命周期「先收集 VLM 页再 start」防竞态、`layout_pages`/`layout_status` 进 stats）、`backend/tools/ab_textlayer_vs_vlm_parse.py`（Route B 接 provider，生产同源）、`backend/tests/test_processor_ocr.py`（`_NoLayout` 桩防测试真起子进程）
- **量化回归结果**（真实 API + 真实 babeldoc 运行时，2026-09-14）：
  1. **FG-RAG p1（根因①③）**：标题 0→5 个（`#` 文档标题 + `## Abstract`/`## CCS Concepts`/`## Keywords`/`## 1 Introduction`——后四者靠 run-in 拆分，VLM 把标题行与正文粘连）；Permission 版权段/`∗Corresponding author.` 脚注残留 **0**（遮罩+区域文本双保险）；「ACM Reference Format」引用块仍保留（已知边界，见下）。
  2. **FG-RAG p3（根因②）**：三张无框表（0.56/0.76/0.92，去重后）全部以 `tab_*` 快照呈现，表格文本全部从 truth 扣除（in_truth=False ×3），`### 3.4`/`## 4`/`### 4.1` 编号层级正确。
  3. **DALK p9**：0.97 置信无框表 → `tab_p009_00.png` 快照，表格专属行（"GPT-3.5-turbo w/ DALK 0.66"）不入 truth——原 bag=0.69 稳定降级页的根因消除。
  4. **泛化（未见论文 TransE 2013，零调参）**：文档标题 0.95→`#`、Abstract 0.91→`##`、"1 Introduction" 0.84→`##`，标题层级三级恢复。
  5. **A/B 全链路**（Route B 含版面信号）：FG-RAG p1/p3 均 src=vlm（生产交叉校验通过）；工具侧 raw 口径 bag 小幅回落（0.997→0.922/0.899→0.882）系**有意剔除内容**（版权段遮罩、表格转快照）仍在工具真值中的口径稀释，非内容丢失——工具 docstring 已注明。
  6. **门禁**：pytest 224 过（+22 例新单测）/ `npx tsc` 0 错 / `compileall` 干净。
- **实测驱动的两处修正**（相对任务草案，均有量化依据）：
  - 置信下限 0.6→**0.5**：0.6 会把 FG-RAG p3 第三张无框表（0.56）连同步复检框（0.38）一起滤掉，违反验收标准 1；0.5 恰落在「Algorithm 伪代码框 ≤0.42」与「真表 ≥0.56」的实测空档内。
  - 新增 **run-in 标题拆分**：任务草案只写「标题提升」，但 VLM 实测常把标题行与后续正文粘成一段（"Abstract Retrieval-Augmented..."），整段提升会吞正文；改为段落前缀与 title 区域文本逐字符对齐（`_norm_split`）即拆——几何证据驱动，非文本猜测，符合协议第 9 条。
- **已知边界（T10 用户拍板项）**：FG-RAG p1「ACM Reference Format」引用块保留——版面模型判 title(0.88)+plain text(0.97) 非 abandon，冻结字号证据 smallprint 不触发（正文字号），剔除只能靠文本规则（协议第 9 条禁止）。候选后续信号源：PP-DocLayoutV3（PaddleOCR-VL 1.5 自带版面模型）换源评估。另：正文页若有 caption 落在 figure 区域边缘，快照外扩可能把 caption 卷进遮罩——walkthrough 时顺带观察。
- **性能**：版面检测 ~1.2s/页 + 首次模型加载 ~2.5s（一次性），与 VLM 并行流式（worker 逐页吐区域，页任务按需等待）；区域按页缓存（doclayout-v1）回归重放零成本。

### 7.3 T10 验收反馈修复：列感知重排（2026-09-15）

用户走查反馈：公式/表格/图片已好、排版有改善，但 **DALK 首页标题与引言顺序乱**（标题排到引言之后）。缓存产物复现定位出四层叠加根因（每层独立可测）：

1. **VLM 整页解析输出本身乱序**：第 0 页原始输出从 `1 Introduction` 开始，文档标题/作者/Abstract 排在中后段（PaddleOCR-VL 免费档阅读顺序偶发问题，模型侧不可修，靠几何重排兜底——这正是 `_column_reading_order` 的设计职责）；
2. **`_match_block` 无锚定包含缺陷**：兜底条件 `t[:24] in h` 对短块恒真——表格残块 `C %`（norm 后仅 `c`）子串命中几乎所有段落（DALK p8 实测 6 段全配到一个 w=17pt 残块，排序 key 全为垃圾坐标）；旧代码靠「块计数不足→整页放弃」碰巧掩盖；
3. **「任一文本段失配即整页放弃重排」过于脆弱**：首页脚注 PDF 原文即拼错（`Constributions`），VLM 输出复数（`Contributions`），单字符漂移→坐标匹配失败→整页维持 VLM 乱序；
4. **栏判定只认块级几何**：首页右栏整栏是一个粗粒度文本块（rights=1 < 3），判不出双栏，重排被拦——而版面模型对该页的语义切分完全正确（左 2 右 2 plain text 区域、栏沟干净 291→306pt）。

**修法**（三处类级通用规则，`backend/ocr/textlayer.py` + `vlm_parse.py`，DALK/FG-RAG 全部 21 个缓存 VLM 页逐一 diff 验证影响面）：

- `_match_block` 匹配锚定：段落头含于块内（`h in t`）或段落以块文本开头（`h.startswith(t[:24])`，VLM 短块+后继块粘连形态）；<4 字符的头/块不参与（防残块垃圾命中）；
- 重排守卫放宽为「少数（≤1/3）失配段跟随邻段排序键（保持原顺序局部性，不跨页乱跳；页首失配段反向跟随后继）」——大面积失配（数学页 LaTeX/参考文献切段，版式未被理解）仍整体放弃，防搅乱纪律不变；
- `_column_reading_order` 增 `col_rects` 参数：块级栏判定失败时用版面模型 plain text 区域兜底（剔除通栏后左右各 ≥2 + 干净栏沟即确证双栏）；textlayer 降级路径不传（无区域信号，行为不变）。`_split_layout_regions` 相应增 `text` 通道。

**回归结果**：21 页全量 old-vs-new diff，**唯一变化 = DALK p1 修复为目标顺序**（`# DALK:...` → 作者 → 单位 → `## Abstract` → `## 1 Introduction` → 正文 → 脚注 → 右栏；页脚 2187/venue 由 abandon 剔除）；其余 20 页（含此前垃圾匹配的 p8）与 v20 线上完全一致。新增 `backend/tests/test_reading_order.py` 6 例（锚定匹配/短块拒绝/少数跟随/多数放弃/区域信号放行/无信号放弃）。`TEXT_LAYER_MODEL v20→v21` 熔断（旧最终产物含乱序缓存）；门禁 pytest 230 过 / tsc 0 错 / compileall 干净。**用户操作**：重翻 DALK 验证（提取缓存自动失效重跑，译文按 text_hash 保留）。

### 7.4 T10 验收反馈修复 2：快照吞正文（prompt 示例框误判图表，2026-09-15）

用户走查反馈：新上传论文（SubgraphRAG，ICLR 版式，29 页）「公式、表格和图片识别处理又很差」。缓存产物逐页复现定位（版面检测 29/29 成功、无崩溃，v20 线上行为）：

- **根因（一类版式的系统性误判，非偶发）**：ICLR 附录的 prompt 示例框 = 带矢量边框的**纯文本块**。`cluster_drawings` 把边框当图表候选（p22-29；个别页模型自己也低置信误判，p5 table@0.58）→ 整块快照 → 三重伤害：①truth 扣除到 48 字符（p25-29）→ 交叉校验失明，VLM 57 字符垃圾输出也 bag=1.0 通过；②遮罩+扣除让 VLM 少认正文（p5 truth 扣 2001 字符 → bag 0.64 → 降级 textlayer → 公式变 Unicode 碎屑）；③降级路径 redact 挖空文本层（最终产物仅 200-300 字符/页）。**DALK p17/p18 附录同款 bug 在 v20 产线潜伏未被发现**。
- **修法（协议第 9 条：信号源优先，不改检测规则阈值）**：版面模型 plain text 区域仲裁**最终快照区域**（不论候选来自几何检测还是模型区域）——区域内文本字符被 plain text 区域覆盖（块主体 ≥50% 面积在内即计入）≥50% 且内部文本 ≥200 字符 → 否决。口径选字符覆盖而非矩形重叠：附录页模型检测碎片化（单行 title/plain 碎框），矩形并集盖不满框内留白（60% 重叠口径实测只否决 1/3）。`_figure_regions`/`_snapshot_figures` 增 `text_regions` 参数，`_prepare` 接线 `lr["text"]`；textlayer 独立路径不传（无版面信号，行为不变）。
- **实测分离度（阈值依据）**：三篇论文全部最终区域——真图真表覆盖 ≤14%（SubgraphRAG p2 Figure1=14%、DALK/FG-RAG 全部表图 0%），prompt 框 ≥60%（p5=0.596 恰为下界）；阈值 0.5 落空档中点。
- **回归结果**（三篇全页快照集 + truth 前后对比）：SubgraphRAG p5/p22-29 共 16 个误快照否决（truth 48→2437 等，恢复整页；p22 真 Table 9（1450 字符）保留）；DALK p17/p18 两个潜伏误快照否决；**FG-RAG 全页零变化**、两篇全部真图真表零变化。
- **熔断**：`VLM_PARSE_MODEL v2→v3`（遮罩集变化：被否决区域不再遮罩；旧原始缓存缺框内文本且 bag 失败后原始缓存永不重取——只在通过校验时落盘，必须整体失效）；`TEXT_LAYER_MODEL` v21 注释补记（v21 尚未部署，用户只失效一次）。
- 新增 `backend/tests/test_snapshot_arbitration.py` 4 例（无信号不越权/正文覆盖否决/未覆盖保留/小区域下限）；门禁 pytest 234 / tsc / compileall 过。**用户操作**：重启后端后重翻 SubgraphRAG 与 DALK 验证（VLM 原始解析整体重取，免费档 ~2-3 分钟；译文按 text_hash 保留）。
- **遗留观察项**：p1/p3/p4 的 VLM 解析 bag 0.71-0.90 未过阈值降级 textlayer（p4 为公式页口径稀释边缘值 0.8952）——修复快照误判后重翻时观察是否自愈（遮罩异常消除后 VLM 输出质量可能恢复）；若仍降级属交叉校验防幻觉纪律的正常代价（内容完整、公式回退碎屑形态），标准 7 走查时专项观察。

### 7.5 T10 补充验收：两项从未人工测试的功能（2026-09-15）

用户发现两个从未走过的验收点，测试结果一好一坏：

1. **主页文献库滚动条 ✅（代码+GUI 双重验证通过，无需修复）**：`Layout.tsx` 非阅读页 main 为 `overflow-y-auto + flex-1 + min-h-0`（h-screen 壳层内的标准滚动容器）。浏览器实测（卡片 DOM 克隆至 60 张模拟大量文献）：容器 `overflowY=auto`、scrollHeight 5914 > 视口 720、**滚动条占位 8px 实际存在**（clientWidth 1032 vs 父容器 1040，滑块高度占比 12% 与 720/5914 吻合，`index.css` 自定义 8px 样式非隐藏）；3 篇时不溢出无滚动条（正确）。局限：测试会话的浏览器输入通道不可用，滚动手势未实际注入，滚动可行性由计算样式+占位+滑块比例数学证明。
2. **导出功能 ❌→✅（启动 API 自 2026-09-12 起全坏，本次修复）**：首次真实调用 POST /api/export/babeldoc 恒 500。根因：`babeldoc_export.start_export` 内 `venv_python = venv_python(...)`——局部赋值与模块级 `venv_python()` 函数同名，Python 把函数内该名字整体局部化，调用即 `UnboundLocalError`（661a85bb 阶段9-T6 重构引入，2026-09-12 22:47 起**每次点导出必崩**，无人发现恰因从未人工测试）。修法：局部改名 `runtime_python`。验证：本地调用返回缓存命中（FG-RAG 09-12 22:08 产物完好：5 页双语；DALK 19 页双语，产于现行 worker 代码 8b6fd85 之后）；新增回归单测（Popen 桩断言启动路径不裸抛且解析结果进 argv[0]）。**注意：运行中的后端仍是旧代码，导出前需重启后端。**

### 7.6 T10 决策落地：导出收敛为两种文件（2026-09-15 用户拍板）

用户决策：导出功能只保留两种文件——**Markdown 译文**（重排版双语内容）+ **BabelDOC 原版对照 PDF**；纯文本格式移除，mono（纯译文版）不进 UI（worker 继续顺带产出，留缓存）。

- **改动**：`src-tauri/commands.rs` 新增 `export_copy_file`（二进制成品复制——`export_content` 只收字符串写不了 PDF；fs 插件未引入，经 Rust 落盘）；`src/utils/export.ts` 收敛 `ExportFormat = markdown | pdf`、新增 `saveDualPdf`（Tauri=save 对话框+复制；浏览器=openLocalPdf 走 /api/file/raw）与 `probeDualPdf`（复用 bridge `checkBabeldocCached`）；`ExportBar` 两格式下拉——PDF 缓存命中直接另存（默认名 `<文档名>-原版对照.pdf`），未生成弹确认引导切到「原版双语对照」模式接力既有生成流程（该模式下工具栏同一下拉导出）；Markdown 默认文件名带文档名（`<文档名>-双语-日期.md`）。
- **验证**：cargo check / tsc / pytest 235 过；三条后端链路直连实测（DALK 探测 cached=true、/api/file/raw 返回 1.8MB application/pdf、SubgraphRAG 探测 cached=false 触发引导分支）。局限：GUI 自动化的输入通道本会话不可用（点击/滚轮均无法注入，与 §7.5 同因），Tauri 另存对话框与生成引导的走查归用户 T10 实测（需先重启后端，§7.5 的启动修复才生效）。
- **交互改版（同日用户样式反馈）**：原「下拉框 + 导出按钮」改为**主色「导出」按钮（btn-primary 蓝色 + 下载图标 + 展开箭头）点开二次选格式**——弹层菜单两项各带图标与一行说明（Markdown 译文「重排版双语内容，可二次编辑」/ PDF·原版对照「BabelDOC 原版排版双语 PDF」），未打开文献时 Markdown 项置灰并提示原因；菜单外点击关闭，选中即执行。样式复用既有约定（index.css btn-primary / MainPage「⋯」菜单同款弹层）。

### 7.7 T10 反馈修复 3：重开文章阅读模式与工具栏选中态脱节（2026-09-15）

用户反馈：紧跟模式下关闭文章再重开，页面是左右对照但工具栏选中态还停在紧跟模式。根因：工具栏「重排版」组选中态读 uiStore `mode`，页面由路由决定，而 `handleOpenDoc` 三个分支（已在看/已有页签/新开）与翻译接管弹窗、TitleBar 页签切换共 4 处硬编码 `navigate("/reader/bilingual")`（新翻译自动跳转 `MainPage:115` 早已是 mode 感知口径）。修法：4 处统一为 `mode === "inline" ? "/reader/inline" : "/reader/bilingual"`——保留用户上次的模式选择，页面与工具栏天然一致；DualPdfPage 的「返回对照」显式置 mode 属有意行为不动。tsc 过。
